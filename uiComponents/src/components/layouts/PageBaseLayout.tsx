import styled from "styled-components";
import { colors } from "../../common";

const PageBaseLayout = styled.div`
  padding: 1rem;
  display: flex;
  flex-direction: column;
`;

export default PageBaseLayout;
